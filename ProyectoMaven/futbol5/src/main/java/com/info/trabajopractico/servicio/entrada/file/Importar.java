package com.info.trabajopractico.servicio.entrada.file;

import com.info.trabajopractico.domain.Jugador;

import java.io.IOException;
import java.nio.file.Path;
import java.nio.file.Paths;
import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;


public class Importar {
    /**
    Path archivo = Paths.get("equipo.txt");
    Scanner lector = new Scanner(archivo);

    //lector.useDelimiter("[;\\n]");

    List<Jugador> listadoJugadores = new ArrayList<>();
//No funcionaaaaaa......
        while (lector.next()) {
        String nom = lector.next();
        String ape = lector.nextLine();
        double medida = lector.nextDouble();
        int goles = lector.nextInt();
        int partidosJugados = lector.nextInt();
        boolean esCapitan = lector.hasNextBoolean();
        int camiseta = lector.nextInt();
        String equipo = lector.next();
        String posicion = lector.next();

        //String linea = lector.nextLine();  otro metodo!!
        //linea.split(";") split devuelve un vector

        Jugador nuevo = new Jugador(nom,ape,medida,goles,partidosJugados,esCapitan,camiseta,equipo,posicion);
        listadoJugadores.add(nuevo);

        System.out.println(nuevo);
        lector.close();
    }
        //lector.close();

    public Importar() throws IOException {
    }
     **/

}
